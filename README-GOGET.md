# check GOBIN & GOPATH for install & import package

$ go get <package> # go get github.com/gorilla/mux

# update
$ go get -u <package>

# verbose
$ go get -v <package>

# SSL certificate problem: certificate has expired
git config --global http.sslVerify false
